{% extends 'base.html.twig' %}

{% block body %}
    <h1>Index</h1>
    <h2>Locale: {{ app.request.locale }}</h2>
    <p>
        <a href="{{ path('page', { page: 'example', '_locale': app.request.locale }) }}">Example</a>
    </p>
{% endblock %}